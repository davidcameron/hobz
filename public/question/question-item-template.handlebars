<div class="row">
  <div class="col-md-12">
    {{#if isFirst}}
      <h2>Question: {{question_text}}</h2>
    {{else}}
      <p>Question: {{question_text}}</p>
    <button type="button" class="js-submit-answer btn btn-link">Give an answer!</button>
    {{/if}}
  </div>
</div>
<div class="row">
  <div class="region-answer-form js-answer-form col-md-6"></div>
</div>
